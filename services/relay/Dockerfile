FROM node:20-alpine

WORKDIR /app

# Fuerza reinstalación cuando cambies el número
ARG CACHE_BUST=1

COPY package.json ./
RUN npm install --omit=dev --no-audit --no-fund

COPY . .

ENV NODE_ENV=production
CMD ["node", "src/index.js"]
